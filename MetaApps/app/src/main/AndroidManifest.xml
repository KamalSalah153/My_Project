<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.apps.metaapps">

    <application
        android:allowBackup="true"
        android:icon="@drawable/m"
        android:label="@string/app_name"
        android:roundIcon="@drawable/mmm"
        android:supportsRtl="true"
        android:theme="@style/Theme.MetaApps">
        <activity android:name=".RandomHighestMountains"></activity>
        <activity android:name=".HomeActivity" />
        <activity android:name=".CalculatorActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ForgetActivity" />
        <activity android:name=".Register" />
        <activity android:name=".SplachScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>